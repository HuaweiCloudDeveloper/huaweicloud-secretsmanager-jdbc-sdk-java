---
version: 2.0

steps:
 pre_codecheck:
    - checkout

tool_params:
  secsolar:
    compile_root_dir:
    compile_script: mvn install -Dmaven.test.skip=true -gs /usr1/maven/settings/cbu-maven-user-settings.xml -s /usr1/maven/settings/cbu-maven-user-settings.xml


